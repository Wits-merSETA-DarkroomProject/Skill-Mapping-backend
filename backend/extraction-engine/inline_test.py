import sys; sys.path.insert(0, r'c:\Users\luten\work\Skill-Mapping-backend\extraction-engine'); from extract import FileExtractor; e = FileExtractor(); r = e.extract_and_save('test-file/mock_cv.pdf'); print('\n'.join([f"{k}: {v}" for k,v in {k:r.get(k) for k in ['success', 'file_name', 'file_type', 'file_size_bytes', 'extracted_at']}.items()]) if r.get('success') else f"Error: {r.get('error')}")
